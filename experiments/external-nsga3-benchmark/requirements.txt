# Standalone benchmark dependencies — kept separate from mo-engine/requirements.txt
# to avoid contaminating the production image with optimization libraries that
# the SimLab platform does not use at runtime.
#
# Pinned to exact versions for reproducibility of the native x DEAP x pymoo
# comparison reported in the paper (Table 3). These match the versions used by
# mo-engine so results are directly comparable.
numpy==2.4.4
scipy==1.18.0
pandas==3.0.2
deap==1.4.4
pymoo==0.6.1.6
