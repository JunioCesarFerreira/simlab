apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    secureJsonData:
      token: my-token
    jsonData:
      version: Flux
      organization: myorg
      defaultBucket: docker
      tlsSkipVerify: true
    isDefault: true
