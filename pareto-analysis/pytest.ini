[pytest]
# `.`  → pareto-analysis/ (for `import plot_pareto_results`, `import lib.*`)
# `..` → repo root (test-only: lets the parity test import `pylib.benchmarks`
#        without coupling the standalone CLI runtime to pylib).
pythonpath = . ..
testpaths = tests
